1、根目录：可访问与不可访问的文件
用户不可以直接访问的文件夹不可访问
可以访问：一齐可以访问的前端文件都是
可访问的代码直接放在根目录，不可访问的代码全部放在protected
2、controller：控制器，lib：model，template：视图
在控制器中，要区分前后台代码。后台要放置admin文件夹
每个模块一个文件夹
3、视图的路径与controller路径是一致，这样做是为了方便对应视图